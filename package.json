{
  "name": "link-extractor",
  "private": true,
  "workspaces": ["web", "api", "cli"],
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm --filter clippr-api run build && pnpm --filter clippr-cli run build",
    "test": "pnpm -r run test"
  }
}
